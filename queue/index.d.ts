export class Queue {
  /**
   * @param app The server.
   * @param clientId Client that sends the actions.
   * @param key Queues's key from BaseServers queues Map.
   */
  constructor(app: BaseServer, clientId: string, key: string)
}
